# aidoc 工具链依赖
# ===========================

# 核心依赖
requests>=2.28.0          # HTTP 客户端（Ollama / OpenAI API 调用）

# PDF 转换（仅 aidoc_convert.py 需要）
docling>=2.0.0
docling-core>=2.0.0

# 可选：GPU 加速（aidoc_convert.py）
# torch                   # CUDA/MPS 加速，按需安装
